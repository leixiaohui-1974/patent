# PF6-1：一种水网控制系统模型在环测试平台及测试方法

## （一）技术领域

本发明属于智慧水利控制验证与仿真测试技术领域，具体涉及一种面向CHS (Cybernetics of Hydro Systems) 与HydroOS平台的水网控制系统模型在环测试（MIL, Model-in-the-Loop）平台及测试方法，涵盖闭环仿真架构、标准场景库、故障注入与性能评估体系。

## （二）背景技术

水网控制策略在实际工程上线前需要充分验证，但真实工程试错成本高、风险大、窗口期短。传统离线分析方法主要检验局部算法指标，难以反映控制器与水动力过程的闭环耦合行为。若缺少系统化测试平台，控制参数在现场调试中易出现超调、振荡和安全边界逼近。

现有测试手段通常存在以下问题：第一，测试对象与仿真对象割裂，无法形成真实闭环；第二，测试场景覆盖不足，极端工况和故障场景缺失；第三，评价指标不统一，难以横向比较不同控制策略；第四，测试过程可追溯性差，复现实验困难。

随着WNAL (Water Network Autonomy Level, L0-L5) 提升，控制系统需具备可重复、可量化、可审计的验证流程。尤其在分层分布式控制与DMPC场景下，必须在上线前验证安全性、稳定性、鲁棒性与实时性。

因此，需要一种面向水网控制系统的MIL平台与测试方法，在纯软件环境下实现"控制器—仿真器—评估器"闭环联测，支撑标准化准入验证。

### 现有技术对比表

| 对比维度 | 现有技术1：Delgoda等(2016) 灌溉渠道MPC离线仿真验证 | 现有技术2：Wahlin & Clemmens(2006) ASCE渠道自动化测试协议 | 本发明 |
|---------|------|------|--------|
| 测试架构 | 控制器与仿真器分离，手工串接 | 定义测试流程但无闭环平台 | 控制器-仿真器-评估器三模块闭环平台 |
| 场景管理 | 单一设计工况 | 标准场景有限（约5类） | 标准化场景库覆盖正常/极端/故障三类 |
| 故障注入 | 不支持 | 不支持 | 支持传感器漂移、执行器饱和、通信时延注入 |
| 评价体系 | 仅IAE或ISE | 仅水位偏差与稳定时间 | 六维统一指标体系含安全违规率 |
| 自动化程度 | 手工运行与记录 | 手工运行 | 全自动编排、批量扫描与报告生成 |
| 可追溯性 | 无版本管理 | 无版本管理 | 版本化归档与审计追溯 |

## （三）发明内容

### 1. 技术问题

本发明要解决的技术问题是：针对水网控制算法缺乏工程级闭环测试平台、场景覆盖不足和评价不统一的问题，提出一种模型在环测试平台及方法，实现控制策略上线前的系统化验证。

### 2. 技术方案

本发明提供一种水网控制系统模型在环测试平台及测试方法，包括如下步骤：

**步骤S1：搭建MIL闭环架构。**

平台包括：
1) 被测控制器模块 $C$；
2) 高精度水动力仿真器模块 $P$；
3) 场景管理与故障注入模块 $S$；
4) 指标评估与报告模块 $E$。

闭环关系：
$$
u_k=C(x_k,r_k),\quad x_{k+1}=P(x_k,u_k,d_k)
$$
其中 $x_k$ 为状态，$u_k$ 为控制量，$r_k$ 为参考轨线，$d_k$ 为扰动。

**步骤S2：构建标准化测试场景库。**

场景库至少包含：
- 正常工况场景（稳态跟踪、计划变流）；
- 极端工况场景（突增/突降来流、需求阶跃）；
- 故障注入场景（传感器漂移、执行器饱和、通信时延）。

每个场景定义输入边界、持续时间、通过阈值与安全约束。

**步骤S3：执行自动化测试编排。**

按测试计划自动加载场景、运行闭环仿真并采集全量日志。支持批量参数扫描：
$$
\Theta=\{\theta_1,\theta_2,\ldots,\theta_n\}
$$
用于比较不同控制参数配置效果。

自动化编排算法伪代码如下：

```
算法：MIL自动化测试编排
输入：场景库L = {s_1, ..., s_M}, 参数集Theta = {theta_1, ..., theta_n}, 控制器C, 仿真器P
输出：测试结果矩阵R[M x n]

1. 初始化结果矩阵 R = empty[M x n]
2. FOR j = 1 TO n DO                          // 遍历参数配置
3.     加载控制器参数 C.set_params(theta_j)
4.     FOR i = 1 TO M DO                       // 遍历场景
5.         加载场景配置 (x_0, r, d, T_end, thresholds) = s_i
6.         初始化仿真器 P.reset(x_0)
7.         k = 0
8.         WHILE k * dt < T_end DO             // 闭环仿真循环
9.             u_k = C.compute(x_k, r_k)
10.            IF s_i.has_fault(k) THEN
11.                d_k = s_i.inject_fault(k, d_k)   // 故障注入
12.            END IF
13.            x_{k+1} = P.step(x_k, u_k, d_k)
14.            记录日志 log.append(k, x_k, u_k, d_k)
15.            k = k + 1
16.        END WHILE
17.        R[i,j] = evaluate(log, thresholds)   // 指标计算
18.    END FOR
19. END FOR
20. RETURN R
```

**步骤S4：建立性能评价指标体系。**

定义核心指标：
$$
J=[e_{track},t_{settle},OS,IAE,E_{ctrl},R_{safe}]
$$
分别表示跟踪误差、调节时间、超调量、绝对误差积分、控制能耗与安全违规率。

各指标计算公式：
$$
e_{track} = \frac{1}{N}\sum_{k=1}^{N}|y_k - r_k|, \quad IAE = \sum_{k=1}^{N}|y_k - r_k|\Delta t
$$
$$
OS = \frac{\max(y_k) - r_{ss}}{r_{ss} - y_0} \times 100\%, \quad R_{safe} = \frac{N_{violation}}{N_{total}}
$$

**步骤S5：通过判定与分级评估。**

通过判定算法伪代码：

```
算法：MIL测试通过判定
输入：测试结果R[i,j], 通过阈值集T = {T_e, T_t, T_os, T_iae, T_ctrl, T_safe}
输出：判定结论verdict, 未通过项列表fail_list

1. fail_list = empty
2. FOR each 场景i, 参数配置j DO
3.     IF R[i,j].R_safe > 0 THEN
4.         fail_list.add("场景i参数j: 安全违规 R_safe=" + R[i,j].R_safe)
5.     END IF
6.     IF R[i,j].e_track > T_e THEN
7.         fail_list.add("场景i参数j: 跟踪误差超限")
8.     END IF
9.     IF R[i,j].t_settle > T_t THEN
10.        fail_list.add("场景i参数j: 调节时间超限")
11.    END IF
12.    // 类似检查OS, IAE, E_ctrl
13. END FOR
14. IF fail_list is empty THEN
15.    verdict = "通过"
16. ELSE
17.    verdict = "未通过"
18.    按指标类型统计未通过项分布
19. END IF
20. RETURN verdict, fail_list
```

若指标满足阈值且安全违规率为0，则判定通过；否则输出未通过项与原因清单。支持A/B策略对比与等级评分。

**步骤S6：生成可追溯测试报告。**

输出包含场景配置、关键曲线、指标统计、故障响应与结论建议的标准报告，并写入版本化测试档案。

### 3. 有益效果

1. 在纯软件环境实现闭环联测，避免现场试错风险；
2. 通过标准场景库提高测试覆盖率与可复现性；
3. 通过统一指标体系实现策略横向可比；
4. 通过自动化编排提升测试效率；
5. 为控制策略上线提供可审计准入依据。

## （四）附图说明

**图1** MIL平台总体架构图。
**图2** 场景库管理与故障注入流程图。
**图3** 闭环仿真与指标评估数据流图。
**图4** 测试报告生成与归档流程图。

## （五）具体实施方式

### 实施例1：渠池水位控制器MIL验证

选取某输水段12个渠池作为测试对象，仿真步长30 s，测试时域48 h。控制器采用基于IDZ传递函数模型的局部MPC。场景库配置20个场景，其中正常场景8个、极端场景6个、故障注入场景6个。

测试结果：
- 跟踪误差均值 $e_{track}=0.047\,m$；
- 调节时间中位数 $t_{settle}=18.5\,min$；
- 安全违规率 $R_{safe}=0$。

判定：通过上线前验证。

### 实施例2：故障注入鲁棒性测试

注入"水位传感器+0.06 m偏置"和"闸门执行器速率受限"复合故障。控制器在2个控制周期内触发约束重配置，系统未发生红线越限。对比未启用故障补偿的基线控制器，超调量下降约41%。

### 实施例3：参数扫描与策略对比

对权重参数 $\theta=[w_h,w_u,w_s]$ 进行网格扫描（共45组），自动输出Pareto候选配置。最终推荐配置在误差与能耗之间取得平衡：IAE较基线降低15.8%，控制能耗增加仅3.2%。

### 对照实验：本发明方法与传统测试方法对比

为验证本发明方法的有效性，在同一输水工程对象上分别采用三种测试方法进行对照实验：

| 对比指标 | 传统离线仿真(人工配置) | 基于脚本的半自动测试 | 本发明MIL闭环平台 |
|---------|------|------|--------|
| 场景数量 | 5个(人工构造) | 12个(脚本批量) | 20个(标准场景库) |
| 故障注入场景 | 0个 | 2个(手动注入) | 6个(自动注入) |
| 测试耗时(48h工况) | 约16工时(含手工记录) | 约6工时 | 约1.5工时(全自动) |
| 场景覆盖率 | 31%(仅典型工况) | 58%(缺极端场景) | 93%(三类全覆盖) |
| 指标一致性 | 不统一(各场景人工定义) | 部分统一 | 六维统一指标体系 |
| 发现问题数 | 2个 | 5个 | 11个(含3个隐蔽故障耦合问题) |
| 可追溯性 | 无(手写记录) | 部分(脚本日志) | 完整(版本化归档+审计链) |
| 统计显著性 | 无(单次运行) | 无 | 每场景重复5次，结果标准差<3% |

实验结果表明，本发明MIL闭环平台在场景覆盖率、测试效率、问题发现能力和结果可追溯性上均显著优于传统方法。

## （六）权利要求书

**权利要求1.** 一种水网控制系统模型在环测试平台及测试方法，其特征在于，包括以下步骤：

步骤一，搭建由被测控制器模块 $C$、高精度水动力仿真器模块 $P$、场景管理与故障注入模块 $S$、指标评估与报告模块 $E$ 组成的闭环测试平台，其闭环关系为 $u_k=C(x_k,r_k)$，$x_{k+1}=P(x_k,u_k,d_k)$，其中 $x_k$ 为状态，$u_k$ 为控制量，$r_k$ 为参考轨线，$d_k$ 为扰动；

步骤二，构建包含正常工况场景、极端工况场景和故障注入场景的标准化测试场景库，每个场景定义输入边界、持续时间、通过阈值与安全约束；

步骤三，按测试计划依次加载场景，在闭环仿真中按步长 $\Delta t$ 迭代执行"控制器计算—故障注入—仿真器推进—日志记录"循环，并支持对控制参数集合 $\Theta=\{\theta_1,\ldots,\theta_n\}$ 的批量扫描；

步骤四，基于六维性能评价指标 $J=[e_{track},t_{settle},OS,IAE,E_{ctrl},R_{safe}]$ 对每个场景-参数组合的闭环响应进行评估；

步骤五，当安全违规率 $R_{safe}=0$ 且其余指标均满足对应阈值时判定测试通过，否则输出未通过项与原因清单；

步骤六，生成包含场景配置、关键曲线、指标统计与结论的版本化测试报告并归档。

**权利要求2.** 根据权利要求1所述的方法，其特征在于，所述故障注入场景至少包括传感器漂移注入、执行器饱和注入和通信时延注入，且支持单一故障与复合故障组合。

**权利要求3.** 根据权利要求1所述的方法，其特征在于，所述跟踪误差按 $e_{track}=\frac{1}{N}\sum_{k=1}^{N}|y_k-r_k|$ 计算，所述超调量按 $OS=\frac{\max(y_k)-r_{ss}}{r_{ss}-y_0}\times 100\%$ 计算，所述安全违规率按 $R_{safe}=N_{violation}/N_{total}$ 计算。

**权利要求4.** 根据权利要求1所述的方法，其特征在于，所述批量参数扫描采用网格扫描或拉丁超立方采样，并输出Pareto候选配置集。

**权利要求5.** 根据权利要求1所述的方法，其特征在于，所述测试平台支持A/B策略对比测试，在相同场景下运行两种控制策略并按统一指标进行差异分析。

**权利要求6.** 根据权利要求1所述的方法，其特征在于，所述标准化测试场景库中每个场景包含场景编号、ODD参数配置、仿真时域、参考轨线、扰动序列与通过阈值。

**权利要求7.** 根据权利要求1所述的方法，其特征在于，每个场景重复运行不少于3次以获取统计显著性，测试结果取均值与标准差。

**权利要求8.** 根据权利要求1所述的方法，其特征在于，所述被测控制器为基于IDZ传递函数模型的模型预测控制器或分布式模型预测控制器。

**权利要求9.** 根据权利要求1所述的方法，其特征在于，所述仿真器为基于Saint-Venant方程离散的明渠水动力模型或管网瞬变流模型。

**权利要求10.** 根据权利要求1所述的方法，其特征在于，所述测试报告以版本化方式归档，包含版本号、测试时间戳、参数快照和审批签名。

**权利要求11.** 根据权利要求1至10任一项所述的方法，其特征在于，所述方法部署于HydroOS平台并用于WNAL L1-L4验证场景。

**权利要求12.** 一种执行权利要求1至11任一项所述方法的水网MIL测试系统，其特征在于，包括闭环仿真模块、场景库模块、故障注入模块、指标评估模块和报告归档模块。

## （七）摘要

本发明公开了一种水网控制系统模型在环测试平台及测试方法。该方法构建由被测控制器、高精度水动力仿真器、场景管理与故障注入、指标评估与报告模块组成的闭环测试平台；通过标准化场景库覆盖正常、极端和故障工况，自动执行批量测试并采集全量日志；基于跟踪误差、调节时间、超调量、绝对误差积分、控制能耗和安全违规率等指标进行统一评估，当安全违规率为零且指标满足阈值时判定通过。该方法可显著提升控制策略上线前验证效率、可复现性与可追溯性。
